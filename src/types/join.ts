export type FilterOption = {
    title: string
    filter: React.ReactNode
}

export type Event = {
    title: string
    img: string
    attendance: number
    cinema: string
    time: Date
    movie: string
}
