import { Bellota } from 'next/font/google'
import { Noto_Sans_TC } from 'next/font/google'

export const bellota = Bellota({ weight: ['700'], subsets: ['latin'] })
export const noto_Sans_TC = Noto_Sans_TC({ subsets: ['latin'] })
